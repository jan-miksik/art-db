# Production Environment
# Copy this to .env.production and fill in your production values
# NEVER commit the actual .env.production file!

DJANGO_ENV=production

# Django Secret Key (REQUIRED - use a strong, unique key)
DJANGO_SECRET_KEY=CHANGE-THIS-TO-A-STRONG-RANDOM-SECRET-KEY

# Debug MUST be False in production
DEBUG=False

# Production domain(s) - comma-separated
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Production Database
PGDATABASE=art_db
PGUSER=postgres
POSTGRES_PASSWORD=CHANGE-THIS-STRONG-PASSWORD
PGHOST=your-db-host
PGPORT=5432

# Production frontend origin(s) - comma-separated
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Production Arweave wallet path (store securely, outside web root)
ARWEAVE_WALLET_PATH=/secure/path/to/arweave_wallet.json
